export function useErrorHandler(error: any) {
  console.error('Error:', error)
  // You can add more sophisticated error handling here
  // like toast notifications, logging to external services, etc.
}
